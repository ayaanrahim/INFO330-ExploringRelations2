select employees.FirstName, employees.LastName, invoices.* from employees inner join customers on employees.EmployeeId=customers.SupportRepId inner join invoices on customers.CustomerId=invoices.CustomerId;
